name: Tests

env:
  IMAGE_NAME: mirror-to-gitea

on:
  push:

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - uses: jdx/mise-action@v2
      - run: task world